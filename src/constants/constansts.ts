export const defaultPageSize = 10
export const defaultPage = 1

export enum Status {
    Active = "Active",
    Inactive = "Inactive",
    Both = "Both",
}

export enum State {
    ADD,
    CHANGE_PASSWORD,
    EDIT,
    DELETE,
}
